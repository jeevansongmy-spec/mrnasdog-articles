# drafts/ — never published

Alternative versions kept for history. `syndicate.py` refuses any file outside
`crypto/` and `growth/`, and every file here carries `published: false` with the
canonical pointing at the live page, so a crawler that finds one is sent to the
original.

- `free-leverage-a/b/c.md` — three draft versions of
  https://mrnasdog.com/analysis/free-leverage (Sep 4 2026). The published mirror
  is `crypto/free-leverage.md`.
